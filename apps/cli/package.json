{
  "name": "@cc-assistant/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "cca": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cc-assistant/client": "workspace:*",
    "@cc-assistant/shared": "workspace:*"
  }
}
